application.title=LogSearch
application.vendor=enclojure
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/LogSearch.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.clojure-contrib.jar=lib/clojure-contrib.jar
file.reference.clojure.jar=lib/clojure.jar
file.reference.congomongo.jar=lib/congomongo.jar
file.reference.lucene-core-3.0.0.jar=lib/lucene-core-3.0.0.jar
file.reference.mongo.jar=lib/mongo.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.lucene-core-3.0.0.jar}:\
    ${file.reference.mongo.jar}:\
    ${file.reference.congomongo.jar}:\
    ${file.reference.clojure-contrib.jar}:\
    ${file.reference.clojure.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=com.spariev.logsearch
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
